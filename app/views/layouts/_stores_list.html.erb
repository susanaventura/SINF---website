<div class="categories">
  <ul>
    <h3>Stores</h3>
    <% stores.each do |store| %>
        <li><%= link_to  store['name'], products_path(codStore: store['code']) %></li>
    <% end %>
  </ul>
</div>